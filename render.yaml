services:
  - type: web
    name: ACE
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn ace_bot:app"
    autoDeploy: true
    branch: main
